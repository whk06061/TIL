<내 코드>

```python
import sys
N = int(input())
for i in range(N):
    word = sys.stdin.readline().rstrip().lower()
    tmp = ""
    for j in word[::-1]:
        tmp += j
    if tmp == word:
        print(f'#{i+1} YES')
    else:
        print(f'#{i+1} NO')
```
